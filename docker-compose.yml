version: '3'
services:
  # Nginx
  rag_web:
    build:
      context: ./
      dockerfile: ./web/Dockerfile
    ports:
      - "86:80"
    depends_on:
      - rag_app
    volumes:
      - ./data:/var/www/html

  # php-fpm-phalocn
  rag_app:
    build:
      context: ./
      dockerfile: ./app/Dockerfile
    env_file: .env
    environment:
      MYSQL_DSN: rag_db
    depends_on:
      - rag_db
    volumes:
      - ./data:/var/www/html
      - ./logs:/var/log/nginx
      - ./app/time_zone.ini:/usr/local/etc/php/conf.d/time_zone.ini

  # MySQL
  rag_db:
    image: mysql:5.7.14
    env_file: .env
    ports:
      - "3308:3306"
    volumes:
      - db-data:/var/lib/mysql

# 永続ボリューム
volumes:
  db-data:
